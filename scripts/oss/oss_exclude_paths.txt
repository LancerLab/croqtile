# Path exclusion rules for open-source sync.
#
# Format:
#   - Lines ending with / are directory prefixes (anything under them).
#   - Lines ending with /* are also directory prefixes (everything inside).
#   - Lines containing * or ? (not trailing /*) are shell-glob patterns
#     where * matches anything EXCEPT /  (single path component).
#   - All other lines are exact path matches.
#   - Empty lines and lines starting with # are ignored.

# GCU target backend
lib/Target/GCU/

# GCU tests
tests/gcu/
tests/parse/intrinsic_call.co

# Benchmarks (internal)
benchmark/

# Internal documentation
Documents/internal/
Documents/plans/
Documents/Documentation/target/
Documents/GPU-Examples/

# External build scripts (GCU toolchain)
extern/Makefile

# Internal scripts
scripts/*.sh
scripts/*.md
scripts/*.awk
scripts/hooks/
scripts/oss/
scripts/sdk/

# Samples (internal)
samples/

# GitLab CI and tokens (internal)
.gitlab-ci.yml
.gitlab/
.gitlab-*.txt

# Internal runtime module
runtime/catz/
runtime/gcu/

# Git metadata (managed separately per repo)
.gitignore
.gitmodules
.gitattributes

# AI agent config (internal skills, codex, claude)
.claude/
.codex/
.github/skills/
.cursor/

# Internal performance results
performance/

# Project meta (managed separately per repo)
AGENTS.md
.clang-format
LICENSE.TXT

# IDE / editor config
.vscode/

# Misc internal artifacts
results.tsv

# CoIR IR tooling -- internal-only paths
tools/coir/lib/CodeGen/GCU/
tools/coir/tests/gcu/
tools/coir/tests/driver/emit-topscc.co

# Dependency config (managed as conflict path, different per branch)
cmake/deps.conf

# External deps (managed separately per repo)
extern/*
