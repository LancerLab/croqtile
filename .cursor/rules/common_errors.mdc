---
description: 
globs: *.co
alwaysApply: false
---
```mdc
# Choreo常见错误摘要

## 语法错误

### 变量声明与初始化
- ❌ 在声明时初始化变量：`u32 x = 5;`
- ✅ 正确方式：先声明后赋值
  ```choreo
  u32 x;
  x = 5;
  ```

### 数组访问
- ❌ 使用方括号访问数组：`array[i][j]`
- ✅ 正确方式：使用`.at()`方法
  ```choreo
  array.at(i, j)
  ```

### 数组声明
- ❌ 多个方括号声明多维数组：`u32 [2][4] array;`
- ✅ 正确方式：单个方括号内用逗号分隔
  ```choreo
  u32 [2, 4] array;
  ```

### 逻辑表达式
- ❌ 使用`&&`或`||`组合条件：`if (a && b)`
- ✅ 正确方式：使用嵌套if语句
  ```choreo
  if (a) {
    if (b) {
      // 执行操作
    }
  }
  ```

## 类型错误

### 索引类型
- ❌ 使用`u32`作为索引类型
- ✅ 正确方式：使用`int`类型作为索引

### 类型不匹配
- ❌ 不同类型之间直接操作：`f32 + u32`
- ✅ 正确方式：进行显式类型转换

## 并行和同步错误

### inthreads条件
- ❌ 使用非发散条件：`inthreads(N > 10)`
- ✅ 正确方式：使用与并行ID相关的条件
  ```choreo
  inthreads(p.id < N)
  ```

### 同步问题
- ❌ 缺少必要的同步：在共享内存操作后未同步
- ✅ 正确方式：使用`sync.shared`或等待事件

## 内存访问错误

### 边界检查
- ❌ 未检查数组访问边界：`array.at(i, j)`（当i或j可能越界）
- ✅ 正确方式：添加边界检查
  ```choreo
  inthreads(i < N && j < M) {
    array.at(i, j) = value;
  }
  ```

### DMA操作
- ❌ 未检查DMA操作的合法性
- ✅ 正确方式：确保源和目标内存区域有效且大小匹配

## 函数相关错误

### 参数声明
- ❌ 函数参数使用多维数组：`void func(f32 [2][3] arr)`
- ✅ 正确方式：使用单维数组或符号化维度

### 返回值
- ❌ 缺少返回值或返回类型不匹配
- ✅ 正确方式：确保函数返回值与声明的返回类型匹配

## 性能问题

### 内存布局
- ❌ 不合理的内存访问模式导致性能下降
- ✅ 正确方式：优化内存布局和访问模式以提高局部性

### 并行度设置
- ❌ 不合理的并行度设置导致资源浪费或不足
- ✅ 正确方式：根据硬件资源和任务特性选择合适的并行度
```