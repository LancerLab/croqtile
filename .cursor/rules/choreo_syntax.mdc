---
description: 
globs: *.co
alwaysApply: false
---
```mdc
# Choreo语法规则摘要

## 基本数据类型
- `f32`: 32位浮点数
- `f16`: 16位浮点数
- `bf16`: 16位脑浮点数
- `u32`: 32位无符号整数
- `s32`: 32位有符号整数
- `u16`: 16位无符号整数
- `s16`: 16位有符号整数
- `u8`: 8位无符号整数
- `s8`: 8位有符号整数
- `bool`: 布尔类型

## 数组和索引
- 数组维度必须在单个方括号内用逗号分隔：`u32 [2, 4]`
- 数组元素访问必须使用`.at()`方法：`array.at(i, j)`
- 索引使用`int`类型，而非`u32`或其他无符号类型

## 变量声明和赋值
- 变量必须先声明后赋值，不能在声明时赋值
- 正确：
  ```choreo
  u32 x;
  x = 5;
  ```
- 错误：
  ```choreo
  u32 x = 5; // 不允许
  ```

## 函数声明
- 使用`__co__`标记Choreo函数
- 函数参数不能包含多维数组声明
- 支持符号化维度：`__co__ auto matmul(f32 [M, K] lhs, f32 [N, K] rhs)`

## 并行结构
- `parallel p by N`：定义并行执行块，其中`N`是并行度，`p`是并行ID
- 可以访问`p.id`获取当前并行实例的ID（从0开始）
- 并行块可以嵌套，例如`parallel p by 2, q by 8`

## 循环和遍历
- `with`语句用于定义有界变量：`with i in [N]`
- `foreach`语句用于遍历有界变量：`foreach i in [N]`

## 条件执行
- `inthreads(条件表达式)`：用于条件执行，类似于if语句
- 条件表达式必须是严格发散的（与并行ID相关）
- `inthreads`块结束后会自动同步所有线程
- `inthreads.async`用于异步条件执行，不会自动同步

## 内存访问
- 使用`.at()`进行数组访问：`array.at(i, j)`
- 使用`.span`获取数组维度
- 使用`.chunkat()`进行数据块访问

## 事件和同步
- `shared event e`：定义共享事件
- `trigger e`：触发事件
- `wait e`：等待事件
- `sync.shared`：同步共享内存

## 常见限制
- 逻辑表达式如`a && b`必须拆分为嵌套if语句
- 索引变量不支持算术运算
- 不支持十六进制常量
- `parallel`块和`foreach`循环必须包含实际操作
```