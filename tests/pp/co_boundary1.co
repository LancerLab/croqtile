// RUN: choreo -E %s | grep -v "\<CHECK\>" | FileCheck --match-full-lines %s


hello/*the crucial*/ world. /*
whatever*/ __cok__ /*here is*/ our { kernel /* raised up
early and */ eat his breakfast // { { { {
} // it is not something new

but at the sametime,
the great  __co__ choreo(/* comes to life and } */) { finds /* many
*/things weird /*but he does not care*/ // }
what a co-incidence/* interesting, //
he/she said*/ } } But actually, this never /*ever*/ happens. It is /*
just /*fiction*/ a noval


// CHECK: hello/*the crucial*/ world. /*
// CHECK: whatever*/  __cok__  our { kernel /* raised up
// CHECK: early and */ eat his breakfast // { { { {
// CHECK: }

// CHECK: but at the sametime,
// CHECK: the great  __co__ choreo()  { finds
// CHECK: things weird
// CHECK: what a co-incidence
// CHECK:  } } But actually, this never  happens. It is
// CHECK: just /*fiction*/ a noval
