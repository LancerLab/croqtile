// RUN: choreo %s | FileCheck %s --match-full-lines
__co__ auto int_ret() { return 3; }

int main() {
  auto res = int_ret();
}

// CHECK:   D(host_func_)("__choreo_int_ret", {StreamType()}, [&](auto args) {
// CHECK:     auto anon_0 = alloc_(output_type);
// CHECK:     return std::vector<Value>{anon_0};
// CHECK:   }, true); // end of choreo-factor host program

// CHECK: int int_ret() {
// CHECK:   __choreo_int_ret(stream, &output);
// CHECK:   return res[0];
// CHECK: }
