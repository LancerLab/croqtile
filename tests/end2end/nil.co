// REQUIRES: TARGET-GCUALL
// SHELL-ONLY
// RUN: choreo -t factor %s -o %s.factor.result && %s.factor.result | FileCheck --match-full-lines %s && rm -f %s.factor.result
// RUN: choreo -t topscc %s -o %s.topscc.result && %s.topscc.result | FileCheck --match-full-lines %s && rm -f %s.topscc.result

__co__ void nil() {}

int main() {
  nil();
  std::cout << "Run Complete.\n";
}

// CHECK: Run Complete.
