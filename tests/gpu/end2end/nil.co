// REQUIRES: TARGET-GPU
// RUN: choreo -t cute %s -o %s.cute.result && %s.cute.result | FileCheck --match-full-lines %s && rm -f %s.cute.result
// RUN-COCR: %cocc -t cute -gs %s | bash -s -- --execute | FileCheck --match-full-lines %s

__co__ void nil() {}

int main() {
  nil();
  std::cout << "Run Complete.\n";
}

// CHECK: Run Complete.
