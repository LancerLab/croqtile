// REQUIRES: TARGET-GPU
// RUN: choreo -t cute -gs %s | bash -s | FileCheck --match-full-lines %s
// RUN: cocc -t cute -gs %s | bash -s -- --execute | FileCheck --match-full-lines %s
// RUN: cocc -t gpu -gs %s | bash -s -- --execute | FileCheck --match-full-lines %s

__co__ void nil() {}

int main() {
  nil();
  std::cout << "Run Complete.\n";
}

// CHECK: Run Complete.
