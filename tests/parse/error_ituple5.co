// RUN: not choreo %s 2>&1 | FileCheck %s

__co__ auto illegal_ituple() {
  ituple it[2] = {7, 8};  // ituple<2> is correct
}

// CHECK: error: syntax error, unexpected [, expecting =
// CHECK-NOT: error:

