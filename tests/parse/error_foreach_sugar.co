// RUN: not choreo %s 2>&1 | FileCheck %s

__co__ auto illegal_foreach_sugar() {
  foreach {x,y} in 2 {}
}

// CHECK: error: syntax error, unexpected NUM, expecting [ or IDENTIFIER
// CHECK-NOT: error:
