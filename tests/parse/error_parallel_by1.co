// RUN: not choreo %s 2>&1 | FileCheck %s

__co__ void i_parallel_by1(int a, int b) {
  parallel p by {6} { }
}

// CHECK: error: must use mdspan instead of ituple to define the parallel bound.
// CHECK-NOT: error:
