// RUN: choreo %s 2>&1 | FileCheck %s

__co__ void foo() {
  parallel by 2 parallel by 1;
  parallel by 3 parallel by 2 parallel by 1; // only valid for gcu4
}

// CHECK: error: too many parallel-by levels: 3 > 2.
