// RUN: not choreo %s 2>&1 | FileCheck %s

__co__ auto StringDecl() {
  a = "abc";
  println(a);
  parallel by 2 {
    b = "xyz";
    println(b);
  }
}

// CHECK: string variables are not supported yet.
// CHECK: string variables are not supported yet.
