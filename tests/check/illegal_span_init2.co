// 1RUN: not choreo %s 2>&1 | FileCheck %s

__co__ auto foo(s8[4,8] i, float x, int y, half h) {
  f16[4,8] o{11.5f};
  parallel by 1 {
    shared s8 [4, 8] s1{x};
  }
  return o;
}

// CHECK: unsupported cast of scalar: f32 => s8
