// RUN: choreo -t cc -sa=check %s 2>&1 | FileCheck %s
// CHECK: warning: {{\[\[}}launch_bounds]] is ignored for non-GPU targets (cute, hip).

__co__ void bad_target() {
  [[launch_bounds(7)]]
  parallel by 1 : block {
  }
}

int main() { return 0; }
