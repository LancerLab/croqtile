// GELU Activation Basic Version 1
__co__ f32[B, S, D] gelu_basic_1(f32[B, S, D] X) {
    f32[B, S, D] Y;
    parallel b by B {
        local f32[S, D] tmp;
        // 1. tmp = gelu(X) (省略实现)
        dma.copy tmp => Y.chunkat(b, _, _);
    }
    return Y;
} 