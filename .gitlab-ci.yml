# 定义 CI/CD 流水线的阶段
stages:
  - gpu-test

# Test 阶段：需要 GPU 支持的测试，运行 `make test` 来执行测试
test:
  stage: test
  script:
    - echo "Running tests with GPU..."
    - make setup && make test  # 假设 `make test` 用于运行测试
  tags:
    - GPU
  only:
    - main 
