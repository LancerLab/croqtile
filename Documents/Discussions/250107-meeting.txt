1. Any special handling of shared DMA? Need more cases

2. Discussion about 'AFTER' semantics. Need improvement.

3. Improvement: DMA at host side
__co__ void foo(f32 [100, 8] a) {
  with idx in [25] {
    foreach idx {
      dma.copy a.chumkat(idx, _) => global;
       parallel p by {
       }
    }
  }
}


4. Symbolic shape of local/shared
// add runtime check, improve the symbolic valno handling
__co__ void foo(f32 [M, N] a) {
  parallel p by 6 {
     a.chunkat(p, _) => local;
  }
}

5. Minimal codesize: runtime check disable functionality

6. Optimization: DMA config - incremental config - opt

7. Allocation of memory - research topic

8. 
