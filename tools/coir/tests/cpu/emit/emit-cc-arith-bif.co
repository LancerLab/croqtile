// RUN: cocc -t cc -es %s -o - | FileCheck %s
//
// Verify arith BIFs (__sqrt, __exp, ...) lower to std:: calls in expressions.

__co__ f32 [4] test_arith_bif(f32 [4] input) {
  f32 [4] output;
  foreach i in 4 {
    output.at(i) = __sqrt(input.at(i));
  }
  return output;
}

// CHECK: std::sqrt(
