// REQUIRES: TARGET-GPU
// RUN: cocc -t cute -gs %s -o %s.result && bash %s.result --execute | FileCheck --match-full-lines %s && rm -f %s.result
// RUN: cocc -t gpu -es %s | FileCheck --check-prefix=PTX %s
//
// Minimal kernel: no params, no return. Tests host wrapper for void kernels.

__co__ void nil() {}

int main() {
  nil();
  std::cout << "Run Complete.\n";
}

// CHECK: Run Complete.
// PTX: .visible .entry nil_kernel
