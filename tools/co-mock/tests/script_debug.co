// RUN: printf 's\ns\np x\nc\n' > %t.script && co-mock -s %t.script %s 2>&1 | FileCheck %s

__co__ void script_debug_test() {
  s32 x = 10;
  s32 y = x + 5;
  println(y);
}

// CHECK: x = 10
// CHECK: 15
